# 创世流式真实进度实施计划

1. 为顶层 JSON 增量扫描器与阶段映射补红灯测试。
2. 实现扫描器、阶段定义及结构/引用修补流程。
3. 增加 `GenesisTask` Prisma 模型和迁移。
4. 实现租约 runner、任务创建/查询/重试 API 与 SSE。
5. 固定创世 Prompt 顶层字段顺序。
6. 改造首页为任务创建后立即跳转。
7. 实现可刷新恢复、SSE 断线轮询兜底的全屏进度页。
8. 执行迁移、单元/集成测试、类型检查、Lint、构建和浏览器体验验证。
